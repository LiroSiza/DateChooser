<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DateChooser" parent="Theme.Material3.DayNight.NoActionBar">
        !-- Cambiar el color primario a blanco -->
        <item name="colorSurface">@android:color/white</item>
        <item name="colorAccent">@android:color/white</item>
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryVariant">@android:color/white</item>
        <item name="colorOnPrimary">@android:color/black</item>

        <!-- Cambiar el color de fondo del layout a blanco -->
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="Theme.DateChooser" parent="Base.Theme.DateChooser" />
</resources>